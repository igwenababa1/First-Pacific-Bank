# Private registry credentials and package resolution policies.
# Google AI Studio builds utilize standard public registry packages directly.
# If you require private enterprise translation or localization SDK registries,
# configure them below.

# Always use package-lock.json for strict dependency resolution
package-lock=true

# Keep logs clean and silent
loglevel=notice
